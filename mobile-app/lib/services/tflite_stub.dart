/// TFLITE STUB FOR WEB
/// Prevents dart:ffi errors when compiling for Chrome.
class Interpreter {
  static Future<Interpreter> fromAsset(String assetPath) async {
    return Interpreter();
  }

  void close() {}
  
  // Add other methods if used in the future
}
