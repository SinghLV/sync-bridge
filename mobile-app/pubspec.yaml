name: sync_bridge_mobile
description: Native Disaster Communication Client with Edge AI.
version: 1.0.0+1
publish_to: 'none'

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  google_fonts: ^6.1.0
  provider: ^6.1.1
  http: ^1.1.0
  shared_preferences: ^2.2.2
  google_generative_ai: ^0.4.0
  firebase_core: ^3.1.0
  cloud_firestore: ^5.0.1
  geolocator: ^12.0.0
  tflite_flutter: ^0.10.4
  image_picker: ^1.0.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
